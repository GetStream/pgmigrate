// Package pgtest provides PostgreSQL integration-test fixtures.
//
// Container-backed helpers are compiled only with the integration build tag so
// the default test suite remains fast and does not require Docker.
package pgtest
